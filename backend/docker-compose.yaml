version: '3.1'

services:

  db:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: petshopDb
      POSTGRES_USER: petshop
      POSTGRES_PASSWORD: OIsQT6$106X$
