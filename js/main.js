window.addEventListener('load', () =>{
    let btnWhats = document.querySelector('.button-float-whats')
    let timeout = 6000;

    setTimeout(() => {        
        btnWhats.classList.add('whatsPopup')
        
    }, timeout);

});

