import type { NextPage } from 'next';
import { Button } from '@chakra-ui/react';

const Home: NextPage = () => {
  return (
    <div>
      <Button bg="primary">Entrar</Button>
    </div>
  );
};

export default Home;
